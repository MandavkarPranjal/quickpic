import { ASCIIArtTool } from "./ascii-tool";

export const metadata = {
    title: "ASCII Art - QuickPic",
    description: "Convert images to ASCII art. Fast and free.",
};

export default function CompressToolPage() {
    return <ASCIIArtTool />;
}
