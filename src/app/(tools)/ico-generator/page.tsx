import { IcoGeneratorTool } from "./ico-generator-tool";

export const metadata = {
    title: "ICO Generator - QuickPic", 
    description: "Generate favicon files from any image. Fast and free.",
};

export default function IcoGeneratorToolPage() {
    return <IcoGeneratorTool />;
}