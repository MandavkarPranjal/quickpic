import { NoiseTool } from "./noise-tool";

export const metadata = {
    title: "Noise Image - QuickPic",
    description: "Add noise to images. Fast and free.",
};

export default function NoiseToolPage() {
    return <NoiseTool />;
}
