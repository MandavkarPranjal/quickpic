import { GradientGeneratorTool } from "./gradient-generator-tool";

export const metadata = {
    title: "Gradient Generator - QuickPic",
    description: "Generate custom gradients with noise and blur effects. Fast and free.",
};

export default function GradientGeneratorToolPage() {
    return <GradientGeneratorTool />;
}